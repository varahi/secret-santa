<div class="nav-scroller py-1 mb-2">
    <nav class="nav d-flex">
        <a class="p-2 text-muted" href="{{ path('app_create_user') }}">{{ 'Create user'|trans }}</a>
        <a class="p-2 text-muted" href="{{ path('app_home') }}">{{ 'Users list'|trans }}</a>
        <a class="p-2 text-muted" href="{{ path('app_send_gift') }}">{{ 'Send gift'|trans }}</a>
    </nav>
</div>